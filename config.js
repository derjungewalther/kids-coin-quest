// Kids Coin Quest — public runtime config.
//
// Paste your Supabase project URL and ANON key here. Both are safe to commit:
// the anon key is protected by Row-Level Security policies in the database,
// so it can only read/write what the logged-in user is allowed to.
//
// Leave both null to run the app in offline-only mode (no login, no cloud
// sync — localStorage only, same as before).
window.SUPABASE_URL = null;       // e.g. 'https://abcd1234.supabase.co'
window.SUPABASE_ANON_KEY = null;  // e.g. 'eyJhbGciOi...'
